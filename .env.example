# Example .env for KaliRootBot
# Copy this file to `.env` and fill real values for secrets before running.

# Telegram settings
TELEGRAM_BOT_TOKEN="8551676429:AAGUVDJyTu64aOtTcVZyGDFQhxYYqUrka2E"     # Required: Bot token from BotFather
TELEGRAM_WEBHOOK_URL="https://kalirootbot.onrender.com/webhook/telegram"  # Optional: public URL for webhook mode (e.g. https://<your-service>/webhook/telegram)
TELEGRAM_WEBHOOK_SECRET="V4ntVw6moXOQa1E5iD4OtIbBTjZ6GjkaaDQSMF3HhuM"  # Optional: secret used to validate Telegram webhook header. Set a random value (use secrets.token_urlsafe(32)) and configure as secret on Render.

# Supabase (Database) settings
SUPABASE_URL="https://subpqyuqwhfmmhbtrqpj.supabase.co"
SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN1YnBxeXVxd2hmbW1oYnRycXBqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQwMjQzNTIsImV4cCI6MjA3OTYwMDM1Mn0.bQKjck1J7IQZlGz0WdXqzygtW205m2LdfvGZp7ATjZc"
SUPABASE_SERVICE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN1YnBxeXVxd2hmbW1oYnRycXBqIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NDAyNDM1MiwiZXhwIjoyMDc5NjAwMzUyfQ.VVlk30_dwIcGz6u7N8BEhMoVypxeYXtF57E1L35ayBQ"

# Groq API (models & embeddings)
GROQ_API_KEY="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"
GROQ_MODEL="llama-3.1-8b-instant"
GROQ_EMBEDDING_MODEL="llama-3.1-8b-instant"                      # Optional: default Groq model used for chat & embeddings
GROQ_EMBEDDING_MODEL=""                # Optional: dedicated Groq model for embeddings (recommended). Leave empty to auto-detect

# Gumroad webhook signature
GUMROAD_WEBHOOK_SECRET="REPLACE_ME_GUMROAD_WEBHOOK_SECRET"  # Required if using Gumroad webhooks

# Server / runtime controls
PORT=8000                                                # Optional: port to bind (default: 8000) used by FastAPI
DEFAULT_CREDITS_ON_REGISTER=0                             # Optional: grant this many credits when a user registers (default 0)
SKIP_ENV_VALIDATION=0                                     # Optional: set 1 if you want to skip strict env validation (dev only)
DELETE_WEBHOOK_ON_POLLING=0                               # Optional: set 1 to auto-delete webhook when running polling locally (dev only). In Render set to 0.
# If set to 1, the app will not delete the webhook on shutdown (useful on hosting platforms to avoid brief outages)
PERSIST_WEBHOOK_ON_SHUTDOWN=1
# Embedding backend options: groq | openai | local | none
EMBEDDING_BACKEND="none"
# Enable Groq chat completions? Set 0 to disable to avoid charges
ENABLE_GROQ_CHAT=1
# A fallback message to return when AI fails
FALLBACK_AI_TEXT="Lo siento, no puedo procesar tu pregunta en este momento. Inténtalo de nuevo más tarde."

# Notes:
# - Do NOT commit secrets into version control. Keep `.env` local and safe.
# - Use `.env.example` to know what to fill in; copy it to `.env`:
#   cp .env.example .env
# - If you are deploying to a hosting provider (Render, Fly, etc.) configure env vars through their UI.
