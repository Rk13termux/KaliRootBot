name: ARCHIVED - Secret scan (disabled)

on:
  workflow_dispatch: {}

# NOTE: This workflow has been archived/disabled by the repository owner. Run manually via the GitHub Actions UI if needed.

jobs:
  gitleaks:
    name: Run gitleaks scanning on the repository
    runs-on: ubuntu-latest
    steps:
    - name: Check out
      uses: actions/checkout@v3
    - name: Run gitleaks
      uses: zricethezav/gitleaks-action@v2
      with:
        args: --repo-path=.
